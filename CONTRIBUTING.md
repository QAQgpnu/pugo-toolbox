# Contributing

感谢你愿意改进浦哥工具箱。

1. Fork 仓库并从 `main` 创建功能分支。
2. 每个工具保持单一、明确、三步内可得到结果。
3. 算法优先放在 `utils/`，页面负责交互和状态。
4. 不添加登录、云函数、API key、广告追踪或未经说明的数据上传。
5. 新增工具时同时补页面四件套、注册信息和自动化测试。
6. 提交前运行 `npm test` 和 `npm run preflight`。

Bug 报告请说明微信开发者工具版本、设备系统、复现步骤、预期结果与实际结果。截图前请遮盖个人信息。
